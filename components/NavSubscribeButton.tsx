// Subscribe button — currently hidden (subscriptions paused). Return null so
// nothing renders in the nav; restore from git history to re-enable.
export function NavSubscribeButton() {
  return null;
}
